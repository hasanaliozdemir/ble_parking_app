import 'package:flutter/material.dart';
import 'package:get/get.dart';

class AutherPage extends StatelessWidget {
  const AutherPage({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}